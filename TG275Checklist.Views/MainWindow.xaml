<mah:MetroWindow x:Class="TG275Checklist.Views.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 xmlns:views="clr-namespace:TG275Checklist.Views"
                 mc:Ignorable="d"
                 Title="Checklist"
                 Height="500"
                 Width="700"
                 WindowStartupLocation="CenterScreen"
                 Background="{DynamicResource MahApps.Brushes.Window.Background}">

    <mah:MetroWindow.RightWindowCommands>
        <mah:WindowCommands>
            <TextBlock Text="{Binding Path=PatientName, FallbackValue='PatientName'}"
                       VerticalAlignment="Center"
                       Margin="5,0"/>
        </mah:WindowCommands>
    </mah:MetroWindow.RightWindowCommands>

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Fonts.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Themes/Dark.Cyan.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MahApps.Metro;component/Styles/Controls.FlatButton.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource MahApps.Brushes.Gray8}">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="20"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Grid.Column="0"
                           Text="Patient: "/>
                <TextBlock Grid.Row="1" Grid.Column="0"
                           Text="Course: "/>
                <TextBlock Grid.Row="2" Grid.Column="0"
                           Text="Plan: "/>
                <TextBlock Grid.Row="0" Grid.Column="1"
                           Text="{Binding PatientName}"/>
                <TextBlock Grid.Row="1" Grid.Column="1"
                           Text="{Binding CourseID}"/>
                <TextBlock Grid.Row="2" Grid.Column="1"
                           Text="{Binding PlanID}"/>
            </Grid>
        </Border>

        <views:PatientSetup DataContext="{Binding PatientSetup}" Grid.Row="1"/>

        <StatusBar Grid.Row="2">
            <StatusBarItem Content="{Binding Status}"/>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <StatusBarItem Content="Refresh"/>
                    <Button Content="Button" Command="{Binding Debugton}" Background="Transparent" Foreground="{StaticResource MahApps.Brushes.IdealForeground}" FontSize="12"/>
                    <Separator Style="{StaticResource MahApps.Styles.Separator.StatusBar}"/>
                    <StatusBarItem Content="User: "/>
                    <StatusBarItem Content="{Binding CurrentUser}"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</mah:MetroWindow>